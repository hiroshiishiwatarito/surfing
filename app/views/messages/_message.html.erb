<div class="message" data-session="<%= session[:guider_id] %>">
  <% guider = Guider.find_by(id: message.from_id) %>
  <p class="<%= message.id %>" id="message_sender"><%= guider.name %></p>
  <p class="<%= message.id %>" id="message_content"><%= message.content %></p>
  <script type="text/javascript">
    var from_id = <%= message.from_id %>;
    var current_guider_id = $('.message').data('session');
    if(from_id == current_guider_id){
      $('p.' + <%= message.id %>).css('text-align', 'right');
    }else{
      $('p.' + <%= message.id %>).css('text-align', 'left');
    }
  </script>
</div>